import React, {useState} from 'react';
import {motion, AnimatePresence} from 'motion/react';

function ServicesCarousel({images}: { images: string[] }) {
	return (
		<div className="overflow-hidden relative">

		</div>
	);
}

export default ServicesCarousel;